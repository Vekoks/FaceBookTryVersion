@model MeetLifeClient.Models.UserPicturesModel
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Pictures</title>
</head>
<body>
    <div>
        @if (User.Identity.Name.Contains(Model.UserName))
        {
            using (Html.BeginForm("PicturesSave", "DetaialUser", FormMethod.Post, new { @enctype = "multipart/form-data" }))
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary("", new { @class = "text-danger" })
                <div class="form-group">
                    <label>Write discriptin</label>
                    <div>
                        <input type="text" id="discriptin" name="Discriptin" />
                    </div>
                </div>
                <div class="form-group">
                    <label>Pictures </label>
                    <div class="col-md-10">
                        <input type="file" id="image" name="Image" />
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-md-offset-1 col-md-10">
                        <input type="submit" class="btn btn-default" value="Upload picture" />
                    </div>
                </div>
            }
        }
        <div id="PostList">
            <table id="TablePictures">
                @if (Model.PostsWithPictures.Count > 3)
                {
                    var i = 0;
            
                    foreach (var post in Model.PostsWithPictures)
                    {
                        i++;
            
                        <tr>
                            @for (int j = i; j < i + 3; j++)
                            {
                                if (j < Model.PostsWithPictures.Count)
                                {
                                    <td>
                                        <div>
                                            @{
                                                var likesId = "ListWithLikes" + @post.PostId;
                                                var ButtonLike = "ButtonLike " + @post.PostId;
                                                var ButtonComment = "ButtonComment " + @post.PostId;
                                                var commendDestrictionId = "CommentDisctriptionId" + @post.PostId;
                                                var CommentPostWithId = "CommentPostWithId" + @post.PostId;
                                            }
            
                                            <a id="UserName" href="/DetaialUser/Details/@post.UserName">@post.UserName</a>
                                            <p>Time: @post.DateOnPost min</p>
                                            <p>Post: @post.DiscriptionPost</p>
                                            <img src="@post.PicturePost" alt="" style="max-height:300px;max-width:300px" />
                                            <button id="CountLike" name="@post.PostId">Like: @post.Likes.Count</button>
                                            <div id="@likesId" class="hidden myBox">
            
                                                @foreach (var like in post.Likes)
                                                {
                                                    <img src="@like.PictureProfile" style="max-height:30px;max-width:60px" />
                                                    <p>@like.Username</p>
                                                }
            
                                            </div>
            
                                            @if (!Model.UserName.Contains(this.User.Identity.Name))
                                            {
                                                <input id="PutLikeButtonId" type="button" name="@ButtonLike" value="Like" />
                                            }
            
                                            @if (!post.IsProfilePicture && Model.UserName == User.Identity.Name)
                                            {
                                                <button id="MekaProfilePicture" name="@post.PostId">Make profile picture</button>
                                            }
            
                                            <div>
                                                <input id="@commendDestrictionId" type="text" name="discriptin" />
                                            </div>
                                            <input id="CommentCreateButtonId" type="button" name="@ButtonComment" value="Save comment" />
                                            <div id="@CommentPostWithId">
            
                                                @foreach (var comment in post.Comments)
                                                {
                                                    <img src="@comment.PictureProfile" style="max-height:30px;max-width:60px" />
                                                    <p>@comment.Username : @comment.Description</p>
                                                }
                                            </div>
                                        </div>
                                    </td>
                                }
                            }
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        @foreach (var post in Model.PostsWithPictures)
                        {
                            <td>
                                <div>
                                    @{
                                        var likesId = "ListWithLikes" + @post.PostId;
                                        var ButtonLike = "ButtonLike " + @post.PostId;
                                        var ButtonComment = "ButtonComment " + @post.PostId;
                                        var commendDestrictionId = "CommentDisctriptionId" + @post.PostId;
                                        var CommentPostWithId = "CommentPostWithId" + @post.PostId;
                                    }
            
                                    <a id="UserName" href="/DetaialUser/Details/@post.UserName">@post.UserName</a>
                                    <p>Time: @post.DateOnPost min</p>
                                    <p>Post: @post.DiscriptionPost</p>
                                    <img src="@post.PicturePost" alt="" style="max-height:300px;max-width:300px" />
                                    <button id="CountLike" name="@post.PostId">Like: @post.Likes.Count</button>
                                    <div id="@likesId" class="hidden myBox">
            
                                        @foreach (var like in post.Likes)
                                        {
                                            <img src="@like.PictureProfile" style="max-height:30px;max-width:60px" />
                                            <p>@like.Username</p>
                                        }
            
                                    </div>
            
                                    @if (!Model.UserName.Contains(this.User.Identity.Name))
                                    {
                                        <input id="PutLikeButtonId" type="button" name="@ButtonLike" value="Like" />
                                    }
            
                                    @if (!post.IsProfilePicture && Model.UserName == User.Identity.Name)
                                    {
                                        <button id="MekaProfilePicture" name="@post.PostId">Make profile picture</button>
                                    }
                                    
                                    <div><input id="@commendDestrictionId" type="text" name="discriptin" /></div>
                                    <input id="CommentCreateButtonId" type="button" name="@ButtonComment" value="Save comment" />
                                    <div id="@CommentPostWithId">
            
                                        @foreach (var comment in post.Comments)
                                        {
                                            <img src="@comment.PictureProfile" style="max-height:30px;max-width:60px" />
                                            <p>@comment.Username : @comment.Description</p>
                                        }
                                    </div>
                                </div>
            
                            </td>
                        }
                    </tr>
                }
            </table>
        </div>
    </div>

    <script src="~/Scripts/ScriptForRefreshPostDetails.js"></script>
    <script src="~/Scripts/ScriptsForButtons.js"></script>

</body>
</html>
